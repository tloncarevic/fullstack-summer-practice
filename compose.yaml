services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=summerpractice'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
